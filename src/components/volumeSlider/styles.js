import styled from 'styled-components';
import {Slider} from '@material-ui/core';
import colors from '../../assets/colors'

export const VolumeSlider = styled(Slider)`
  color: ${colors.tomato} !important;
`;